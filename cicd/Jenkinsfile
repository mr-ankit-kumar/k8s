pipeline {
    agent any
    parameters {
		string(name: 'BASTION_SERVER_IP', defaultValue: '', description: 'Bastion server IP to deploy')
    }

	environment {
		BASTION_SERVER_IP = ""
		PATH_F = "/var/jenkins_home/workspace/demo"
    }
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                sh 'cd configure-pods/'            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
		sh 'ls -lrth'
		sh 'pwd'
            }
        }
        stage('Print Message') {
            steps {
                script {
			dir(PATH_F){
	                    def BASTION_SERVER_IP = params.BASTION_SERVER_IP
	                    echo BASTION_SERVER_IP
			    sh 'cat abc.txt'
                	}
		}
	    }
	}
    }
}
